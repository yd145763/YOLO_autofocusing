# Train/val/test sets as 
## 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]


#path: ../datasets/coco128  # dataset root dir
train: /home/grouptan/Documents/yudian/yolo_microscope/images/train  # train images (relative to 'path') 128 images
val: /home/grouptan/Documents/yudian/yolo_microscope/images/val # val images (relative to 'path') 128 images
# test:  # test images (optional)

# Classes
nc: 4  # number of classes
names: ['Region A', 'Region B', 'Region C', 'Region D']  # class names  # class names